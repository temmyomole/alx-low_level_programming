#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - Prints the len of a string
 * @s: The memory address of string
 *
 * Return: Always 0 (Success)
 */
int _strlen(char *s)
{
	int k = strlen(s);

	return (k);
}
