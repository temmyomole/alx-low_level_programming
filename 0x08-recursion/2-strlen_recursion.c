#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - A function that returns the length of a string.
 * @s: string which length is to be gotten
 * Return: Always 0
 */

int _strlen_recursion(char *s)

{

	return (strlen(s));

}
