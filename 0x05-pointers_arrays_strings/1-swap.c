#include "main.h"
#include <stdio.h>

/**
 * swap_int - swaps the values of two integers
 * @a: int a
 * @b: int b
 * Return: void
 */
void swap_int(int *a, int *b);
{
	int m = *b;
	int n = *a;

	*a = m;
	*b = n;
}
